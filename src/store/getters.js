const config = state => state.app.config
const markers = state => state.app.markers
const authenticatedUser = state => state.app.authenticatedUser

export {
  config,
  markers,
  authenticatedUser
}
